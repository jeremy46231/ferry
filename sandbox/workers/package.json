{
  "name": "ferry-sandbox-workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "ln -sf ../../.env .dev.vars && bun install",
    "dev": "wrangler dev --port 5173"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.5.2",
    "wrangler": "^4.111.0"
  }
}
